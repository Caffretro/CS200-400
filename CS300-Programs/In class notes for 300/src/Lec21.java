import java.util.Queue;
import Lec19.BinaryTreeNode;

public class Lec21 {
    public String toString(int depth) {
        String s = "";
        Queue<BinaryTreeNode> q = new Queue<>();
        q.enqueue(this);
        while(!q.isEmpty()) {
            
        }
        
    }
}
